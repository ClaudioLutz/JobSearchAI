# JobSearchAI Environment Configuration
# Copy this file to .env and fill in your actual values
# DO NOT commit .env file to version control!

# =============================================================================
# Gmail SMTP Configuration for Email Sending
# =============================================================================
# These credentials are required for sending job application emails via Gmail.
#
# SETUP INSTRUCTIONS FOR GMAIL APP PASSWORD:
# 1. Go to your Google Account settings: https://myaccount.google.com/
# 2. Navigate to Security > 2-Step Verification (must be enabled first)
# 3. Scroll down and click on "App passwords"
# 4. Select app: "Mail" and device: "Other (Custom name)"
# 5. Enter a custom name like "JobSearchAI" and click "Generate"
# 6. Copy the 16-character password generated (remove spaces)
# 7. Paste it in GMAIL_APP_PASSWORD below
#
# SECURITY NOTES:
# - App passwords only work when 2-Step Verification is enabled
# - Each app password is unique and can be revoked independently
# - Never share or commit these credentials to version control
# - Use a dedicated Gmail account for automated sending (recommended)
#
# Example values (replace with your actual credentials):
GMAIL_ADDRESS=your.email@gmail.com
GMAIL_APP_PASSWORD=abcdefghijklmnop

# =============================================================================
# Flask Application Configuration
# =============================================================================
SECRET_KEY=your-secret-key-here-change-in-production
FLASK_ENV=development
FLASK_DEBUG=True

# =============================================================================
# Database Configuration
# =============================================================================
DATABASE_URL=sqlite:///instance/jobsearchai.db

# =============================================================================
# OpenAI API Configuration
# =============================================================================
OPENAI_API_KEY=your-openai-api-key-here

# =============================================================================
# Optional Configuration
# =============================================================================
# Email sending rate limiting (emails per hour)
# EMAIL_RATE_LIMIT=50

# Email retry configuration
# EMAIL_RETRY_ATTEMPTS=3
# EMAIL_RETRY_DELAY=5

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO
